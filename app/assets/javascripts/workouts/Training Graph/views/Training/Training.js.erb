$(document).ready(function() {	
	var user_id = $("#tabContainer").data("user");
	var margin = {top: 20, right: 40, bottom: 40, left: 00};
	var view = "Training";

	if ($("#tabContainer").length == 1) { draw(900, 360, margin, 25, user_id, view); }		
}); 
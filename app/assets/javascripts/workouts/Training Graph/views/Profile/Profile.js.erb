$(document).ready(function () {	
	var user_id = $(".workouts_graph").data("user_id");
	var margin = {top: 0, right: 0, bottom: 20, left: 00};
	var view = "Profile"; 

	if ($(".workouts_graph").length == 1) { draw(500, 100, margin, 45, user_id, view);}
}); 
